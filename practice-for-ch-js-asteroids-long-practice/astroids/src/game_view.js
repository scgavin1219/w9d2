import Game from "./game";

class GameView{

    constructor(){
        this.game = new Game()
        this.ctx = canvas.getContext("2d");


    }
    //installs key listeners to move hsip and fire bullets
    //installs a timer to call game.prototype.step setInterval?
}

